// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/redhat-appstudio/quality-studio/pkg/storage/ent/db/predicate"
	"github.com/redhat-appstudio/quality-studio/pkg/storage/ent/db/pullrequests"
	"github.com/redhat-appstudio/quality-studio/pkg/storage/ent/db/repository"
)

// PullRequestsQuery is the builder for querying PullRequests entities.
type PullRequestsQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.PullRequests
	withPrs    *RepositoryQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PullRequestsQuery builder.
func (prq *PullRequestsQuery) Where(ps ...predicate.PullRequests) *PullRequestsQuery {
	prq.predicates = append(prq.predicates, ps...)
	return prq
}

// Limit the number of records to be returned by this query.
func (prq *PullRequestsQuery) Limit(limit int) *PullRequestsQuery {
	prq.ctx.Limit = &limit
	return prq
}

// Offset to start from.
func (prq *PullRequestsQuery) Offset(offset int) *PullRequestsQuery {
	prq.ctx.Offset = &offset
	return prq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prq *PullRequestsQuery) Unique(unique bool) *PullRequestsQuery {
	prq.ctx.Unique = &unique
	return prq
}

// Order specifies how the records should be ordered.
func (prq *PullRequestsQuery) Order(o ...OrderFunc) *PullRequestsQuery {
	prq.order = append(prq.order, o...)
	return prq
}

// QueryPrs chains the current query on the "prs" edge.
func (prq *PullRequestsQuery) QueryPrs() *RepositoryQuery {
	query := (&RepositoryClient{config: prq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := prq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := prq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pullrequests.Table, pullrequests.FieldID, selector),
			sqlgraph.To(repository.Table, repository.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pullrequests.PrsTable, pullrequests.PrsColumn),
		)
		fromU = sqlgraph.SetNeighbors(prq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PullRequests entity from the query.
// Returns a *NotFoundError when no PullRequests was found.
func (prq *PullRequestsQuery) First(ctx context.Context) (*PullRequests, error) {
	nodes, err := prq.Limit(1).All(setContextOp(ctx, prq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pullrequests.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prq *PullRequestsQuery) FirstX(ctx context.Context) *PullRequests {
	node, err := prq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PullRequests ID from the query.
// Returns a *NotFoundError when no PullRequests ID was found.
func (prq *PullRequestsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prq.Limit(1).IDs(setContextOp(ctx, prq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pullrequests.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prq *PullRequestsQuery) FirstIDX(ctx context.Context) int {
	id, err := prq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PullRequests entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PullRequests entity is found.
// Returns a *NotFoundError when no PullRequests entities are found.
func (prq *PullRequestsQuery) Only(ctx context.Context) (*PullRequests, error) {
	nodes, err := prq.Limit(2).All(setContextOp(ctx, prq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pullrequests.Label}
	default:
		return nil, &NotSingularError{pullrequests.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prq *PullRequestsQuery) OnlyX(ctx context.Context) *PullRequests {
	node, err := prq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PullRequests ID in the query.
// Returns a *NotSingularError when more than one PullRequests ID is found.
// Returns a *NotFoundError when no entities are found.
func (prq *PullRequestsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prq.Limit(2).IDs(setContextOp(ctx, prq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pullrequests.Label}
	default:
		err = &NotSingularError{pullrequests.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prq *PullRequestsQuery) OnlyIDX(ctx context.Context) int {
	id, err := prq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PullRequestsSlice.
func (prq *PullRequestsQuery) All(ctx context.Context) ([]*PullRequests, error) {
	ctx = setContextOp(ctx, prq.ctx, "All")
	if err := prq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PullRequests, *PullRequestsQuery]()
	return withInterceptors[[]*PullRequests](ctx, prq, qr, prq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prq *PullRequestsQuery) AllX(ctx context.Context) []*PullRequests {
	nodes, err := prq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PullRequests IDs.
func (prq *PullRequestsQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	ctx = setContextOp(ctx, prq.ctx, "IDs")
	if err := prq.Select(pullrequests.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prq *PullRequestsQuery) IDsX(ctx context.Context) []int {
	ids, err := prq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prq *PullRequestsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prq.ctx, "Count")
	if err := prq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prq, querierCount[*PullRequestsQuery](), prq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prq *PullRequestsQuery) CountX(ctx context.Context) int {
	count, err := prq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prq *PullRequestsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prq.ctx, "Exist")
	switch _, err := prq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prq *PullRequestsQuery) ExistX(ctx context.Context) bool {
	exist, err := prq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PullRequestsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prq *PullRequestsQuery) Clone() *PullRequestsQuery {
	if prq == nil {
		return nil
	}
	return &PullRequestsQuery{
		config:     prq.config,
		ctx:        prq.ctx.Clone(),
		order:      append([]OrderFunc{}, prq.order...),
		inters:     append([]Interceptor{}, prq.inters...),
		predicates: append([]predicate.PullRequests{}, prq.predicates...),
		withPrs:    prq.withPrs.Clone(),
		// clone intermediate query.
		sql:  prq.sql.Clone(),
		path: prq.path,
	}
}

// WithPrs tells the query-builder to eager-load the nodes that are connected to
// the "prs" edge. The optional arguments are used to configure the query builder of the edge.
func (prq *PullRequestsQuery) WithPrs(opts ...func(*RepositoryQuery)) *PullRequestsQuery {
	query := (&RepositoryClient{config: prq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	prq.withPrs = query
	return prq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PrID uuid.UUID `json:"pr_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PullRequests.Query().
//		GroupBy(pullrequests.FieldPrID).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (prq *PullRequestsQuery) GroupBy(field string, fields ...string) *PullRequestsGroupBy {
	prq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PullRequestsGroupBy{build: prq}
	grbuild.flds = &prq.ctx.Fields
	grbuild.label = pullrequests.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PrID uuid.UUID `json:"pr_id,omitempty"`
//	}
//
//	client.PullRequests.Query().
//		Select(pullrequests.FieldPrID).
//		Scan(ctx, &v)
func (prq *PullRequestsQuery) Select(fields ...string) *PullRequestsSelect {
	prq.ctx.Fields = append(prq.ctx.Fields, fields...)
	sbuild := &PullRequestsSelect{PullRequestsQuery: prq}
	sbuild.label = pullrequests.Label
	sbuild.flds, sbuild.scan = &prq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PullRequestsSelect configured with the given aggregations.
func (prq *PullRequestsQuery) Aggregate(fns ...AggregateFunc) *PullRequestsSelect {
	return prq.Select().Aggregate(fns...)
}

func (prq *PullRequestsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prq); err != nil {
				return err
			}
		}
	}
	for _, f := range prq.ctx.Fields {
		if !pullrequests.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if prq.path != nil {
		prev, err := prq.path(ctx)
		if err != nil {
			return err
		}
		prq.sql = prev
	}
	return nil
}

func (prq *PullRequestsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PullRequests, error) {
	var (
		nodes       = []*PullRequests{}
		withFKs     = prq.withFKs
		_spec       = prq.querySpec()
		loadedTypes = [1]bool{
			prq.withPrs != nil,
		}
	)
	if prq.withPrs != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, pullrequests.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PullRequests).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PullRequests{config: prq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := prq.withPrs; query != nil {
		if err := prq.loadPrs(ctx, query, nodes, nil,
			func(n *PullRequests, e *Repository) { n.Edges.Prs = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (prq *PullRequestsQuery) loadPrs(ctx context.Context, query *RepositoryQuery, nodes []*PullRequests, init func(*PullRequests), assign func(*PullRequests, *Repository)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*PullRequests)
	for i := range nodes {
		if nodes[i].repository_prs == nil {
			continue
		}
		fk := *nodes[i].repository_prs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(repository.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "repository_prs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (prq *PullRequestsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prq.querySpec()
	_spec.Node.Columns = prq.ctx.Fields
	if len(prq.ctx.Fields) > 0 {
		_spec.Unique = prq.ctx.Unique != nil && *prq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prq.driver, _spec)
}

func (prq *PullRequestsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   pullrequests.Table,
			Columns: pullrequests.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: pullrequests.FieldID,
			},
		},
		From:   prq.sql,
		Unique: true,
	}
	if unique := prq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := prq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pullrequests.FieldID)
		for i := range fields {
			if fields[i] != pullrequests.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := prq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prq *PullRequestsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prq.driver.Dialect())
	t1 := builder.Table(pullrequests.Table)
	columns := prq.ctx.Fields
	if len(columns) == 0 {
		columns = pullrequests.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prq.sql != nil {
		selector = prq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prq.ctx.Unique != nil && *prq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range prq.predicates {
		p(selector)
	}
	for _, p := range prq.order {
		p(selector)
	}
	if offset := prq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PullRequestsGroupBy is the group-by builder for PullRequests entities.
type PullRequestsGroupBy struct {
	selector
	build *PullRequestsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prgb *PullRequestsGroupBy) Aggregate(fns ...AggregateFunc) *PullRequestsGroupBy {
	prgb.fns = append(prgb.fns, fns...)
	return prgb
}

// Scan applies the selector query and scans the result into the given value.
func (prgb *PullRequestsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prgb.build.ctx, "GroupBy")
	if err := prgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PullRequestsQuery, *PullRequestsGroupBy](ctx, prgb.build, prgb, prgb.build.inters, v)
}

func (prgb *PullRequestsGroupBy) sqlScan(ctx context.Context, root *PullRequestsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prgb.fns))
	for _, fn := range prgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prgb.flds)+len(prgb.fns))
		for _, f := range *prgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PullRequestsSelect is the builder for selecting fields of PullRequests entities.
type PullRequestsSelect struct {
	*PullRequestsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prs *PullRequestsSelect) Aggregate(fns ...AggregateFunc) *PullRequestsSelect {
	prs.fns = append(prs.fns, fns...)
	return prs
}

// Scan applies the selector query and scans the result into the given value.
func (prs *PullRequestsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prs.ctx, "Select")
	if err := prs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PullRequestsQuery, *PullRequestsSelect](ctx, prs.PullRequestsQuery, prs, prs.inters, v)
}

func (prs *PullRequestsSelect) sqlScan(ctx context.Context, root *PullRequestsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prs.fns))
	for _, fn := range prs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
